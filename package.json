{
  "name": "hanbun-lang",
  "version": "1.0.0",
  "devDependencies": {
    "parcel": "^2.1.0",
    "prettier": "^2.5.1",
    "purs-tidy": "^0.6.4"
  },
  "scripts": {
    "format": "purs-tidy format-in-place src/**/*.purs && prettier --write src/**/*.js",
    "docs:build": "spago build && parcel build src/demo/index.html --dist-dir docs --public-url ."
  },
  "author": "Wenqi He",
  "license": "ISC"
}
